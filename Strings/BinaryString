//Given a string s, the task is to check if it is a binary string or not.
// A binary string is a string which only contains the characters '0' and '1'.

public class BinaryString {
    public static boolean checkBinary(String s){
        for(int i=0; i<s.length(); i++) {
            if(s.charAt(i) != '0' && s.charAt(i) != '1'){
                return false;
            }
        }
        return true;
    }
    public static void main(String[] args) {
        String s = "101";
        System.out.println("Given String is Binary : "+checkBinary(s));
    }
}

// output : Given String is Binary : true
